# Windows application-bundle toolchain. Keep the hooks release synchronized
# with PyInstaller so clean GitHub runners produce the same dependency graph.
pyinstaller==6.22.2
pyinstaller-hooks-contrib==2026.7
